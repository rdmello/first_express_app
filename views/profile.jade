extends layout

block content
  h1= title
  p Welcome to #{title}
  p.
    ID: #{user.id}
    Username: #{user.username}
  p Time is #{(function(){var d=new Date(); return d.getSeconds()})()}

  form(action="#")
    input#m(autocomplete="off")
    button#mybtn Send

  ul#messages

  script(src="socket.io/socket.io.js")
  script(src="/resources/jquery.js")
  script.
    var userString = "#{user.username}"; 
  script(src="javascripts/profile_socket.js")
  
